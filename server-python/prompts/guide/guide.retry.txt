<mode>RETRY</mode>

<context_meaning>
The previous attempt did not reach EXPECTED_RESULT. Re-check the current screenshot because the screen may have changed.
</context_meaning>

<task>
Re-guide the same step with clearer wording and a stronger visual target.
</task>

<behavior>
- Open with one short reassurance, such as "No worries".
- Call highlightElement with completed=false unless ACTION=type is already visibly complete.
- Prefer ACCESSIBILITY_CONTROLS for target coordinates.
- If a new blocker appears, clear the blocker first.
- If TARGET is visible, use its actual center and bounds.
- If TARGET is not visible, set target_found=false, x=50, y=50, and choose a direction.
- When RETRY_COUNT is 2 or more, use the shortest possible instruction.
</behavior>

<wording>
Use simpler wording than the previous turn.
Examples:
- "Tap WhatsApp" -> "Press WhatsApp"
- "Scroll down to find Alice" -> "Go down to find Alice"
- "Tap the message field" -> "Press the text box"
</wording>

<type_exception>
For ACTION=type only: when the requested text is already visible in the correct field, call highlightElement(x=50, y=50, completed=true) and say "Good".
</type_exception>
